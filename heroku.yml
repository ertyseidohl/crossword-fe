build:
  docker:
    web:
       dockerfile: Dockerfile
       target: production
run:
  web: /bin/sh -c "envsubst < /etc/nginx/conf.d/site.template > /etc/nginx/conf.d/default.conf && exec nginx -g 'daemon off;'"
